version: "2"
services:
  critical:
    image: critical-css-generator_critical:latest
    #build: .
    ports:
      - "3000:3000"
    environment:
      - URL=${URL:?err}
      - DIMENSIONS=$DIMENSIONS
      - LOCATION=$LOCATION
